import NavBar from '../../components/NavBar';
import Catalogue from './Catalogue';

const index = () => {
  return (
    <div>
      <NavBar />
      <Catalogue />
    </div>
  );
};

export default index;
